name: Reverse Dependency Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  revdep_check:
    name: Reverse Dependency Check
    uses: r-devel/recheck/.github/workflows/recheck.yml@v1
    with:
      which: strong
      subdirectory: ''
